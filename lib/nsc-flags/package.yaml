name: nsc-flags
version: !include "../../version.yaml"

library:
  source-dirs: src

default-extensions: !include "../../default-extensions.yaml"

dependencies:
- base >=4.7 && <5
- optparse-applicative
- extra
- containers
- megaparsec
